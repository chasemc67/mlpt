import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "org.mlpt.training",
  appName: "MLPT",
  webDir: "out",
  backgroundColor: "#101c30",
  ios: { contentInset: "automatic" },
};
export default config;
